# Queue driver configuration
queue {
    default "redis"

    settings {
        queue        "default"
        retry_after  90
        max_attempts 3
        backoff      5
    }

    stores {
        redis {
            host     "redis"
            port     6379
            timeout  2.0
            database 1
        }
    }
}
